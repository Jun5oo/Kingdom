// 추후에 수정이 필요할 것으로 예상됨
/// <summary>패시브 효과 종류. PassiveFactory가 이 값을 기반으로 IPassive 인스턴스를 생성한다.</summary>
public enum PassiveType
{
    None, 
    GainAbilityCoin, 
    SummonGraveyard, 
    DestroySelf
}
